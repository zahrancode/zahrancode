<?php
include "config.php";

// Ambil data profile
$profile = $conn->query("SELECT * FROM profile LIMIT 1")->fetch_assoc();

// Ambil link aktif urut berdasarkan klik terbanyak
$links = $conn->query("SELECT * FROM links WHERE status='active' ORDER BY clicks DESC");

// Total klik
$totalClicks = $conn->query("SELECT SUM(clicks) as total FROM links")->fetch_assoc()['total'];
?>

<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title><?php echo $profile['name'] ?? '3MS Profile'; ?></title>

<link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@600&family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">

<style>
body{
    margin:0;
    font-family:'Poppins',sans-serif;
    background:linear-gradient(135deg,#0f0c29,#302b63,#24243e);
    color:white;
    text-align:center;
    padding:40px 20px;
}

/* PROFILE IMAGE */
.profile-img{
    width:120px;
    height:120px;
    border-radius:50%;
    border:3px solid #00ffff;
    box-shadow:0 0 25px #00ffff;
    object-fit:cover;
}

/* NAME */
h1{
    font-family:'Orbitron';
    margin-top:20px;
    font-size:24px;
    letter-spacing:2px;
}

/* BADGE */
.badge{
    display:inline-block;
    padding:6px 12px;
    font-size:12px;
    border-radius:20px;
    background:linear-gradient(90deg,#ff00cc,#00ffff);
    margin-top:10px;
}

/* BIO */
.bio{
    margin-top:15px;
    font-size:14px;
    opacity:0.8;
}

/* STATS */
.stats{
    margin-top:20px;
    font-size:13px;
    opacity:0.7;
}

/* LINK BUTTON */
.link-btn{
    display:block;
    margin:15px auto;
    width:90%;
    max-width:400px;
    padding:15px;
    border-radius:12px;
    text-decoration:none;
    font-weight:600;
    color:white;
    background:rgba(255,255,255,0.1);
    border:1px solid rgba(255,255,255,0.2);
    backdrop-filter:blur(10px);
    transition:0.3s;
}

.link-btn:hover{
    background:linear-gradient(90deg,#ff00cc,#00ffff);
    transform:scale(1.05);
    box-shadow:0 0 20px #00ffff;
}

/* FOOTER */
.footer{
    margin-top:40px;
    font-size:12px;
    opacity:0.5;
}
</style>
</head>

<body>

<?php if(!empty($profile['photo'])){ ?>
<img src="<?php echo $profile['photo']; ?>" class="profile-img">
<?php } ?>

<h1><?php echo $profile['name'] ?? '3MS GAMING'; ?></h1>

<div class="badge">✔ Official Profile</div>

<div class="bio">
<?php echo nl2br($profile['bio'] ?? 'Digital Gaming & Payment Solution'); ?>
</div>

<div class="stats">
🔥 Total Click: <?php echo $totalClicks ?? 0; ?>
</div>

<?php while($row = $links->fetch_assoc()){ ?>
<a href="go.php?id=<?php echo $row['id']; ?>" class="link-btn">
<?php echo $row['title']; ?>
</a>
<?php } ?>

<div class="footer">
Powered by 3MS Cyber System
</div>

</body>
</html>
